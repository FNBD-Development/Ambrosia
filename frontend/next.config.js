/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
        domains: [
          "images.unsplash.com",
          "localhost:3000",
          "localhost:3001",
          "ambrosia.novabot.eu.org",
          "upload.wikimedia.org",
          "teaclient.net"
        ],
      },
}

module.exports = nextConfig
