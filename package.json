{
  "name": "test",
  "private": true,
  "type": "module",
  "devDependencies": {
    "bun-types": "latest",
    "turbo": "^1.11.2",
    "@ambrosia": "workspace:*"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier \"**/*.{ts,tsx,md}\"",
    "setup": "turbo run setup"
  },
  "workspaces": [
    "/frontend",
    "/backend"
  ]
}
